# API Configuration
# Set to 'true' untuk menggunakan mock data (development tanpa backend)
# Set to 'false' untuk menggunakan API asli melalui proxy
VITE_USE_MOCKS=true

# Optional: Base URL untuk API asli (jika tidak menggunakan proxy)
# VITE_API_BASE_URL=https://api-fintracker.suissetiawan.my.id
